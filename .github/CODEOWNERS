# Every change needs the owner's review by default.
*                       @Enkhoder

# The release workflow is the trust anchor for PyPI Trusted Publishing: anyone
# who can merge a change here can publish under this account.
/.github/               @Enkhoder

# The encoding, key derivation and decoder. A wrong change here silently
# produces images that cannot be decoded, or leaks a seed phrase.
/spicebag/core/         @Enkhoder
/spicebag/utils/        @Enkhoder
/spicebag/constants/    @Enkhoder

# Packaging metadata and the dependency set.
/pyproject.toml         @Enkhoder
/requirements.txt       @Enkhoder
/SECURITY.md            @Enkhoder